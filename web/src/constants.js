// Physics constants (scaled from Unity values for Phaser pixel space)
// Unity used very large values; we scale to typical Phaser arcade physics
export const GRAVITY = 800;
export const FRICTION = 600;

export const VIEWPORT = { width: 1280, height: 720 };

// World bounds (large scrolling level)
export const WORLD = { width: 6000, height: 1200 };
